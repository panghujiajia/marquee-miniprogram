<view class="awardWrap">
    <view bindtap="start" class="start awardBox">开始抽奖</view>

    <view class="awardBox {{currentIndex == item.siteNum ? 'cur' : ''}}" wx:for="{{awards}}">
        {{item.name}}
    </view>
</view>